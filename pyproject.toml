

name = "mcp-with-client"
version = "0.1.0"
description = "understanding MCP 2.0"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [

name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cursor>=1.3.5",
    "mcp>=1.28.0",
]
